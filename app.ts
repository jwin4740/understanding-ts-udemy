function add(n1: number, n2: number){
    return n1 + n2;
}

const number1 = 5;
const number2 = 2.8;

const result = add(number2, number1);
console.log(result);

// if one variable is a string and the other a number; js concatenates them